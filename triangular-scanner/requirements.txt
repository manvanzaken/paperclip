# Minimum-version pins (not exact) so wheels exist for the running Python.
# Python 3.14 needs pydantic >= 2.13 (older pydantic-core has PyO3 < Python 3.14 support).
aiohttp>=3.10
websockets>=12
pydantic>=2.13
ccxt>=4.4
rich>=13
PyYAML>=6.0.2
pytest>=8.2
pytest-asyncio>=0.23
